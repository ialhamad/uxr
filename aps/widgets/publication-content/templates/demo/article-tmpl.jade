extends  ../article/article-tmpl

block articleRow
    .row
        div(class=contentClasses)
            .row
                div
                    | !{pb.render("breadcrumbs", "article.json")}
            | !{pb.render("content-navigation", "demo/content-navigation-article", "content.json")}
            //- +articleHEeader()
            //- +articleCitation()
            block includeCitation
                include ../article/extended/ext-citation-tmpl
                +epub()
            //- +articleCoolbar()
            block includeCoolbar
                .scroll-to-target.fixed-element
                    include ext-coolbar-demo-tmpl
            +articleBody()

        block tabsContainer
            div(class=sidebarClasses)
                block socialLinkDropZone
                //- +articleTab()
                block includeTabs
                    include ext-tabs-demo-tmpl