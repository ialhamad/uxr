query {
  journal {
    code
    addFavorite: link(service: addFavoritePublication)
    currentIssue: latestIssue {
      link(service: showCurrent)
    }
    aop: aopIssue {
      link(service: showToc)
    }
    ja: jaIssue {
      link(service: showToc)
    }
  }
}